//
// Created by x3r1x on 07.02.2026.
//

#include <stdio.h>

int main(void)
{
    int a, b, sum;

    scanf("%d %d", &a, &b); //считываение двух переменных decimal через пробел
    sum = a + b;
    printf("%d\n", sum); //вывод одной переменной decimal и перевод строки

    return 0;
}